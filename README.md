# nginx

A basic(small) nginx server, primary purpose is to provide a mechanism to
easily configure complex nginx deployments.  This container holds examples of
configurations for other deployments to use.

## Proxy

## WebDAV

...


